apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "polkadot.fullname" . }}-test-connection"
  labels:
{{ include "polkadot.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "polkadot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
